const keyForLanguageToLearnChoice = "LanguageToLearn";

// Set up for a new Game.
function initializeGame() {
  // Clear Local Storage of Past Items
  var x = 1;
  //
  //saveUserLanguageSelection("Speanish")
}

// Save the user's the
function saveUserLanguageSelectionGotoGame(languageToLearn) {
  location.replace("./madlibgame.html");
}
